import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'example-ionic-behavior-subject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
